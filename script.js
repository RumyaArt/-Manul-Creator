var window = document.getElementById("window"),
    ctx     = window.getContext('2d');
    ctx.fillStyle = "white";
    ctx.fillRect(0, 0, window.width, window.height);



var element = {
    base: Image,
    stroke_panel: Image,
    additional_element1: Image,
    additional_element2: Image,
    additional_element3: Image
};

